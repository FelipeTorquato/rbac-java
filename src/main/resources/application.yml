spring:
  application:
    name: rbac

  datasource:
    url: jdbc:postgresql://localhost:5432/rbac_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

jwt:
  secret: 0kbkeYHbuGvgNGRo12POe8jJHKAD8HIau1ZhyndfZ89
  expiration: 86400000
